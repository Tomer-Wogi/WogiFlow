# Code Review Report

**Date**: 2026-02-25 14:00
**Task**: wf-f837a262 — Centralized script resolver
**Commit**: 40984ef
**Files Reviewed**: 11
**Review Mode**: parallel (3 core agents)
**Fix Task**: wf-dc1ac7e5

## Verification Gates
- Syntax: All 10 files pass `node --check`
- Tests: pass

## Findings

| # | Severity | Issue | File | Status |
|---|----------|-------|------|--------|
| 1 | Critical | Shell injection — testCmd in execSync template | flow-task-enforcer.js:991 | Fixed |
| 2 | High | {script:X}/{exec:X} template syntax not implemented | config.json | Fixed |
| 3 | High | Script name metacharacter injection | flow-script-resolver.js | Fixed |
| 4 | High | Config cache no invalidation | flow-script-resolver.js | Fixed (documented) |
| 5 | Medium | Raw JSON.parse instead of safeJsonParse | flow-script-resolver.js | Fixed |
| 6 | Medium | getExecParts split assumption | flow-script-resolver.js | Fixed |
| 7 | Medium | Gates cache no invalidation | flow-verify.js | Fixed (documented) |
| 8 | Medium | Duplicate detectPackageManager logic | flow-script-resolver.js + flow-workflow.js | Accepted |
| 9 | Low | Unused exports | flow-script-resolver.js | Accepted |

## Fixes Applied
1. **Shell injection fix**: Replaced `execSync` template literal with `execFileSync` + array args in flow-task-enforcer.js
2. **Template syntax removal**: Replaced all `{script:X}` and `{exec:X}` references in config.json and installer.js with actual commands (`npm run lint`, `npx tsc`, etc.) and `null` for auto-detect
3. **Metacharacter sanitization**: Added `isSafeScriptName()` validator with `UNSAFE_CHARS` regex, applied in `getCommand()` and `getExecParts()`
4. **safeJsonParse**: Replaced raw `JSON.parse` in `getConfig()` and `getPackageScripts()` with `safeJsonParse()` from flow-utils.js
5. **Split guard**: Added `parts.length !== 2` guard in `getExecParts()` with safe fallback
6. **Cache documentation**: Added comments explaining cache lifecycle (fresh Node process per hook invocation)

## Standards Compliance
- All violations found were pre-existing (not introduced by this commit)
- No new naming convention or security pattern violations introduced

## Summary
- Findings: 9 total
- Fixed: 7 | Accepted risk: 2
- All verification gates passing after fixes
