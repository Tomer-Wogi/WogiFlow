# [wf-94cc3b72] Epic — Lift WogiFlow from C+ to A

**Priority**: P1
**Level**: L0 (Epic)
**Type**: epic
**Created**: 2026-04-15
**Source audit**: `.workflow/audits/2026-04-15-pre-igr-project.md` — C+ (78) baseline, adversary-verified

## Epic goal

Move the WogiFlow codebase from **C+ (78) → A (90+)** on the IGR-enhanced `/wogi-audit` rubric, measured against the same Gate 0 baseline and 7-dimension scoring used in the 2026-04-15 audit. A single re-audit at the end should produce a score of A or higher.

## Why A not A+

Gate 0 caps the overall score at 97 (penalty for the 2 lint-config downgrades). Fixing those downgrades (story 1 below) lifts the cap to 100. From there, the work that lifts the dimensions to A+ (94+) is a ~2x cost multiple vs the work that lifts to A (90). Diminishing returns kick in around A/A-. If A is reached early, individual stories can continue toward A+ on a case-by-case basis — but this epic's commit is A, not A+.

## Framing

- **Interpretation**: "Get us to A+" → close the 10 HIGH + top-priority MEDIUM findings from the pre-IGR audit; leave documented/acceptable LOWs. Re-audit to verify.
- **Scope in**: Each story targets one or more specific audit findings with evidence-tier traceability.
- **Scope out**: No new features, no new config keys beyond what existing findings mandate. No refactoring for aesthetic preference.

## Stories (15)

Ordered by **impact × low risk** — front-load cheap wins, back-load structural refactors.

### L0 Architect Amendments (2026-04-22)

The L0 IGR architect pass (saved at `.workflow/state/framing/wf-94cc3b72-architect.md`) flagged 5 issues. Amendments applied inline below, summarized here for traceability:

- **Story 6 moved**: from Wave B position to after Wave E (avoids re-touching files D/E restructure).
- **Stories 9, 10, 12, 13, 14**: characterization tests for extracted modules MUST co-land in the same PR as the extraction. Wave F (Story 15) re-scoped to remaining-legacy gates only.
- **Story 12 hardened**: `process.emitWarning` deprecation per re-exported symbol + CI check that flow-utils.js only contains re-exports. Codemod for 302 importers explicitly deferred (barrel naturally shrinks as new code uses extracted modules directly).
- **Story 12 rollback procedure**: explicit revert instructions added.
- **Stories 4, 5**: cloud-compatibility check added (dual-repo contract — behavior changes in `lib/workspace-*.js` require `partner-versions.json` coordination).
- **Story 7 perf guard**: permanent hyperfine-in-CI regression check added.
- **Story 8 heuristic gap**: documented as pragmatic limitation.
- **Story 1 ratchet**: already incorporated in wf-33ae5671 spec.

### Wave A: Ship-immediately (1 day, mostly mechanical)

**Story 1 [wf-33ae5671] — Re-enable strict lint + delete dead exports (P0)**
- Fix: `cons-c05` — upgrade `no-unused-vars` and `no-unreachable` from `warn` to `error` in `eslint.config.js`
- Fix: `arch-010` — delete 5 dead exports in `flow-constants.js` (LOCK_STALE, LOCK_RETRY_DELAY, etc.)
- Fix: `td-f11` — create `.env.example` with documented API key envs
- Fix: `td-f09` — delete merged stale branch `origin/team-features-backup`; file GitHub issue for the other two stale branches
- Fix: lint outlier catch names (`parseError`, `_parseErr`, etc. → `err` / `_err`) per `cons-c01`
- Evidence target: `npx eslint` clean; `git branch -r` no stale merged branches; `.env.example` present
- **Gate 0 impact**: score cap 97 → 100 (unblocks A+)

**Story 2 [wf-f50fe4f5] — Outlier catch-variable sweep (P1)**
- Fix: `cons-c01` — 16 outlier catch names across flow-providers.js, flow-orchestrate-llm.js, lib/workspace-session.js. Rename to `err` or `_err`.
- Evidence target: `grep -rn 'catch\s*(\s*[a-zA-Z_][^_e][^r][^r]' scripts/ lib/` = 0 matches
- Consistency improvement: +1 tier

### Wave B: Abstraction consolidation (2–3 days, moderate risk)

**Story 3 [wf-789a8cba] — Consolidate duplicate config/utility impls (P1)**
- Fix: `dup-001` — remove 4 loadConfig() wrappers; callers go directly to getConfig()
- Fix: `dup-003` — unify 4 slugify() impls + 8 inline repeats → one `slugify` in flow-utils
- Fix: `dup-005` — unify getProjectRoot() / findProjectRoot() across scripts/ + lib/utils
- Fix: `dup-006` — remove flow-model-config.js `readConfig()` (uses getConfig())
- Fix: `dup-007` — remove flow-epics.js `loadReadyData()` (use `getReadyData()`)
- Evidence target: `grep -rn 'function loadConfig\|function slugify\|function readConfig\|function loadReadyData' scripts/ lib/` = 1 each
- IGR required: **YES** (L1 refactor, architect + adversary pass)

**Story 4 [wf-9fc4970b] — Consolidate safe-JSON + DANGEROUS_KEYS (P1, security)**
- Fix: `dup-002` — 7 DANGEROUS_KEYS redefs → single import from flow-io
- Fix: `dup-004` — 3 safeParseJson impls in lib/ + scripts/postinstall.js → import from flow-io (or document why lib/ can't, then add a minimal shared lib/safe-json.js that scripts/ re-exports)
- Missed issue (adversary-added): raw JSON.parse on subprocess output in lib/workspace-sync.js, lib/workspace-session.js — prototype-pollution risk
- Evidence target: standards-gate clean on all flagged files; no HIGH findings on "raw JSON.parse on untrusted data"
- **Cloud-compat check (Architect 2026-04-22)**: this touches `lib/workspace-*.js` which is part of the dual-repo interface contract. Before release: (1) diff the behavioral change (DANGEROUS_KEYS filtering is a new guard), (2) verify wogiflow-cloud does not depend on raw-JSON-parse semantics, (3) bump `.workflow/state/partner-versions.json` if any behavior observable to cloud changes.
- IGR required: **YES** (security-adjacent, L1)

**Story 5 [wf-8308221a] — Replace 137 raw JSON.parse sites in lib/workspace-*.js (P1)**
- Fix: `cons-c02` — lib/workspace-sync.js (8 raw), lib/workspace-session.js (7 raw), lib/workspace-messages.js (7 raw)
- Use `safeJsonParseString` from flow-io (or lib-local if circular-dep forbids)
- Evidence target: `grep 'JSON.parse' lib/workspace-*.js | wc -l` = 0
- **Cloud-compat check (Architect 2026-04-22)**: same as Story 4. `lib/workspace-*.js` is in the dual-repo interface contract. `safeJsonParseString` differs observably from raw JSON.parse (rejects `__proto__`/`constructor`/`prototype` keys). Before release: diff behavioral change, check wogiflow-cloud, bump `partner-versions.json` if cloud observably affected.
- IGR required: **YES** (security, L1)

**Story 6 [wf-a47ae604] — Replace inline fs.mkdirSync + magic numbers with named helpers (P2) — RESEQUENCED to post-Wave E**
- Fix: `dup-009` — 15 inline `30000` → `QUICK_COMMAND_TIMEOUT_MS`
- Fix: `dup-011` — 99 inline `fs.mkdirSync(dir, { recursive: true })` → `ensureDir(dir)` from flow-io
- Evidence target: grep counts drop to 0 in scripts/; lib/ tracked separately if needed
- Mechanical, low risk
- **Sequencing note (Architect 2026-04-22)**: this story's 99 sites overlap with files Wave D/E restructure (pre-tool-use.js, session-start.js, stop.js, flow-utils.js, flow-durable-session.js, flow-orchestrate.js). Running Story 6 before D/E forces re-touching the same lines twice. **Execution order moved to AFTER Wave E** so the sweep runs against the post-extraction file layout.

### Wave C: Hot-path + performance (2 days, measured impact)

**Story 7 [wf-baf7f52c] — Route hook fast-path through hook-status aggregator (P1)**
- Fix: `perf-003` (routing-gate reads 2 flag files per invocation — use hook-status.routing.cleared)
- Fix: `perf-007` (phase-read-gate reads workflow-phase.json + phase-reads.json — use hook-status.phase)
- Fix: `perf-006` (bugfix-scope-gate bypasses getReadyData cache)
- Target: reduce PreToolUse hot-path sync I/O from 10–14 ops to ≤4 ops on the common Edit/Write/Bash case
- Evidence target: benchmark before/after via `hyperfine` on a representative PreToolUse invocation; <2ms p95 on warm repo
- **Permanent perf guard (Architect 2026-04-22)**: add `tests/flow-hooks-perf.test.js` that runs a representative PreToolUse invocation N times and asserts p95 < 3ms. Fails CI on regression. One-shot benchmarks always decay; permanent guard is the institution.
- IGR required: **YES** (hot-path change, L1)

**Story 8 [wf-255e541a] — Migrate sync fs in async contexts to fs/promises (P2, phased)**
- Fix: `mod-m01` — top-5 offenders first: lib/workspace.js (46 sync), scripts/flow-community.js (50), lib/installer.js (33), scripts/flow-skill-generator.js (31), scripts/flow-adaptive-learning.js (29)
- Rule: **only files that already declare async functions** (others are fine sync — they're not blocking anything)
- Evidence target: `grep 'readFileSync\|writeFileSync' lib/workspace.js scripts/flow-community.js ...` near-zero in files with async declarations
- **Known heuristic gap (Architect 2026-04-22)**: "files with async declarations" misses sync-called-from-async callchains (a sync function imported by async code is equally event-loop-blocking). Full taint analysis is out of scope. Documented as pragmatic limitation; tracked as future work if/when a regression surfaces. The top-5 targets here are all direct-async-fs cases, where the heuristic is accurate.
- IGR required: **YES** (L1, changes event-loop behavior in workspace orchestration)

### Wave D: Architecture (3–4 days, highest impact, most risk)

**Story 9 [wf-5e94e2c0] — Extract pre-tool-use.js orchestration to hook core (P1)**
- Fix: `arch-001` — 560 LOC + 84 branches in entry layer
- Extract: gate chain, subagent detection, agent-type validation, read-only git whitelisting, damage-control dispatch → scripts/hooks/core/pre-tool-orchestrator.js
- Entry file becomes <100 LOC (parse-and-dispatch)
- Evidence target: pre-tool-use.js ≤ 100 LOC
- **Co-land tests (Architect 2026-04-22)**: characterization tests for `pre-tool-orchestrator.js` MUST ship in the SAME PR as the extraction. Write the tests first against pre-extraction behavior (they'll pass on current code), then extract — tests should continue passing with zero changes. This replaces the Wave F approach of "tests after everything."
- IGR required: **YES** (L1, three-layer compliance)

**Story 10 [wf-c1e892fa] — Extract session-start.js + stop.js business logic to core (P1)**
- Fix: `arch-005` — session-start 307 LOC inline drift-detection, plugin-scanning, version-compat → core
- Fix: `arch-008` — stop.js 188 LOC inline curl/git-diff/session-history → core
- Fix: `td-f06` — TD-006 open: CLAUDE.md drift detection in session-start (same as arch-005)
- Evidence target: both entry files ≤ 100 LOC; core module exposes testable functions
- **Co-land tests (Architect 2026-04-22)**: same as Story 9 — characterization tests for the extracted core modules MUST ship in the same PR as the extraction.
- IGR required: **YES** (L1)

**Story 11 [wf-0f2e0f16] — Hook three-layer compliance lint rule (P1, adversary-added)**
- Missed issue (adversary): no lint rule prevents arch-001/005/008 regressions
- Add: `.claude/rules/architecture/hook-three-layer.md` (doc) + eslint custom rule OR a standards-gate check in flow-standards-checker.js that flags any entry file importing scripts/hooks/core/*.js without going through a dispatch function
- Evidence target: rule fires on a synthetic violation test; clean on the refactored entries from stories 9 + 10
- **This story depends on stories 9 + 10 passing first so the lint rule starts clean**

### Wave E: God-file decomposition (5–7 days, high impact)

**Story 12 [wf-c0d6b0c5] — Decompose flow-utils.js per TD-005 (P1)**
- Fix: `arch-006` + `td-f04` — 1,748 LOC / 70+ exports / 302 importers
- Extract: flow-task-ids.js, flow-git-utils.js, flow-request-log.js, flow-ast-grep.js per TD-005 plan
- **Critical**: maintain the existing flow-utils.js as a re-export barrel for backward compat. Never break the 302 existing importers in one PR.
- Evidence target: flow-utils.js < 400 LOC (barrel only); each extracted module single-responsibility
- **Barrel hardening (Architect 2026-04-22)**: (a) each re-exported symbol emits `process.emitWarning('<name> moved to <new-module>', 'DeprecationWarning')` on first access; (b) new CI check `tests/flow-utils-barrel.test.js` asserts flow-utils.js contains ONLY re-exports (no new function definitions, no inline logic) — fails if file grows. Without (b), the barrel becomes a permanent god-facade and arch-006 is only cosmetically closed. Codemod to rewrite the 302 importers to point at extracted modules directly is **explicitly out of scope** (separate initiative) — the barrel naturally shrinks as new code uses extracted modules directly.
- **Co-land tests (Architect 2026-04-22)**: characterization tests for each extracted module (flow-task-ids, flow-git-utils, flow-request-log, flow-ast-grep) MUST ship in the same PR as the extraction.
- **Rollback procedure (Architect 2026-04-22)**: (1) `git revert <extraction-sha>`; (2) if users have already started importing from extracted modules, keep the extracted modules in place and restore inline impls in flow-utils.js; (3) re-enable process.emitWarning only after the decision is stable. Barrel re-export API is the clean rollback point.
- IGR required: **YES** (L0 sub-epic; run architect + adversary on the decomposition plan before touching code)

**Story 13 [wf-33a0aa88] — Decompose flow-durable-session.js per TD-004 (P1)**
- Fix: `td-f03` — 1,802 LOC / 53 fns
- Split: flow-durable-session-core.js, flow-durable-suspension.js, flow-durable-history.js per TD-004
- Evidence target: each sub-module < 600 LOC
- **Co-land tests (Architect 2026-04-22)**: characterization tests for the three new modules MUST ship in the same PR as the extraction.
- IGR required: **YES** (L1)

**Story 14 [wf-d0937c83] — Decompose flow-orchestrate.js autoCorrectCode per TD-002 (P1)**
- Fix: `td-f02` — 125 LOC, 18 branches
- Split into: fixImportPaths, fixQuoteStyle, fixCnImport in flow-orchestrate-corrections.js
- Evidence target: no single function > 50 LOC in the correction path
- **Co-land tests (Architect 2026-04-22)**: characterization tests for `flow-orchestrate-corrections.js` MUST ship in the same PR as the extraction.
- IGR required: **YES** (L1)

### Wave F: Coverage + verification (2–3 days)

**Story 15 [wf-a97af500] — Test coverage for remaining legacy gates (P1) — RESCOPED by Architect 2026-04-22**
- Fix: `td-f01` partial — hook-core test coverage gap.
- **Rescope**: extracted cores from Stories 9/10/12/13/14 will have characterization tests co-landed in-PR. This story covers ONLY the gates NOT touched by the extractions: any of routing-gate, phase-read-gate, task-gate, scope-gate, bugfix-scope-gate, deploy-gate, strike-gate, completion-truth-gate, standards-gate, todowrite-gate that remain untested after 9/10/12/13/14 land.
- **Target**: public-API behavioral tests per remaining gate (not a volume target). Success = each remaining gate has ≥3 behavioral tests covering its pass/block/error paths.
- Outcome metric: coverage moves from 6.9% to the target set by the union of (9/10/12/13/14 co-landed tests) + (this story's remaining-gate tests). No fixed ≥100 number.
- IGR required: **NO** (test-only, non-architectural)

### Completion gate

**Re-audit via `/wogi-audit`** after all 15 stories land. Criteria:
- HIGH count ≤ 2 (down from 10)
- MEDIUM count ≤ 15 (down from 41)
- Adversary verdict: ACCEPT (not REVISE_SCORE)
- Overall score: A (90+)
- Cross-story regression: 1042+ tests still passing

If score lands A- or below, triage remaining HIGH/MEDIUM into a Wave G sub-epic. If score lands A, close the epic.

## Dependencies

- Story 11 depends on 9 + 10
- Story 12 should land before 13 (flow-utils is imported by flow-durable-session)
- Stories 1–5 can run in any order (Story 6 resequenced — see below)
- **Story 6 RESEQUENCED (Architect 2026-04-22)**: now runs AFTER Wave E (Stories 12-14) to avoid re-touching files that are being restructured. Execution order: A → B(3,4,5) → C → D → E → 6 → F(15).
- Story 15 (rescoped) runs after Stories 9/10/12/13/14 since their co-landed tests reduce Story 15's scope.

## Out of scope

- A+ push (94+) — if earned during this epic's stories, great; not a commit
- Cross-repo contract audit with wogiflow-cloud (separate epic per adversary's missed-issue)
- Full-file `fs.promises` migration across all 1,884 sync sites (this epic: top-5 offenders only)
- Bundle-size reduction (@huggingface/transformers 439MB) — needs separate spike
- AGPL license re-evaluation
- Subdirectory reorganization of flow-*.js (TD-010, deferred to major version)
- Reducing console.log count (cons-c06, acceptable for CLI)

## Boundaries (DO NOT MODIFY)

- Do not break the 302 flow-utils importers in one PR (story 12 must keep barrel)
- Do not remove the `lazy-require` wrappers in flow-learning-orchestrator.js (mod-m05 — architecturally documented)
- Do not touch wogiflow-cloud contracts without partner-versions.json update (per dual-repo-management.md)

## IGR pass required

**YES — L0 epic.** Run architect + adversary on THIS epic file before decomposing into stories in ready.json. Additionally, stories 3, 4, 5, 7, 8, 9, 10, 12, 13, 14 each require per-story IGR. Stories 1, 2, 6, 11, 15 are mechanical / test-only / depend-on-others and can skip IGR (still hit standards-gate).

## Measurement

The re-audit at the end is the scorecard. No other success metric counts.

Additionally, track per-story `before/after` evidence citations so each story's contribution is visible in the R-entries.
