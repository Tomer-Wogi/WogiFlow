<!-- PINS: entities, relationships, lifecycle-states, schema-format -->
<!-- reviewStatus: {{reviewStatus}} -->
<!-- lastAutoUpdated: {{lastAutoUpdated}} -->

# Domain Model — {{projectName}}

_Draft generated by IGR Intent Bootstrap. Schema format detected: **{{schemaFormat}}**._

## Entities
<!-- PIN: entities -->

{{#if entities.length}}
{{#each entities}}
### `{{name}}` ({{kind}})
**Location**: {{file}}:{{line}}
**Fields** ({{fieldCount}}): `{{fieldSample}}`{{#if truncated}}, ...{{/if}}
**[CONFIRM]** _Describe what this entity represents in the product and its role in user flows._

{{/each}}
{{else}}
_(No schema-based entities auto-detected. **[CONFIRM]** List the core entities of your domain here.)_
{{/if}}

## Relationships
<!-- PIN: relationships -->

**[CONFIRM]** How do entities relate? Which own which? Which are polymorphic? Which are multi-tenant?

## Lifecycle states
<!-- PIN: lifecycle-states -->

**[CONFIRM]** For entities with states (orders, tasks, invoices, etc.), describe transitions here.

{{#if enums.length}}
_Auto-detected enum types (may be lifecycle states)_:
{{#each enums}}
- `{{name}}` — values: `{{values}}` ({{file}}:{{line}})
{{/each}}
{{/if}}

## Schema format
<!-- PIN: schema-format -->

Detected: `{{schemaFormat}}` ({{filesScanned}} files scanned).
