<!-- PINS: terms, trap-zone -->
<!-- reviewStatus: {{reviewStatus}} -->
<!-- lastAutoUpdated: {{lastAutoUpdated}} -->

# Glossary — {{projectName}}

_Draft generated by IGR Intent Bootstrap. The **Trap Zone** section below is auto-detected structural ambiguity — same term, different meanings. Review carefully; these are the terms that cause logic bugs._

## Terms of art
<!-- PIN: terms -->

**[CONFIRM]** Domain-specific terms that have particular meaning in this project. List terms whose casual-English meaning differs from the in-product meaning.

| Term | In-product meaning | Casual-English meaning (if different) |
|------|---------------------|---------------------------------------|
| **[CONFIRM]** |  |  |

## Trap Zone — ambiguous terms
<!-- PIN: trap-zone -->

{{#if trapZones.length}}
The following terms appear with **structurally different definitions** in multiple places. These are the highest-risk ambiguity sources. The Logic Adversary (IGR Stage 4) reads this section and treats these as mandatory-resolution terms in every task.

{{#each trapZones}}
### `{{term}}`

{{#each occurrences}}
- **{{kind}}** — `{{file}}:{{line}}` — {{fieldCount}} fields: `{{fieldSample}}`{{#if truncated}}, ...{{/if}}
{{/each}}

**[CONFIRM]** — Which of these is the "primary" meaning? What's the disambiguator users / developers should apply? Example:
> _"When context is admin org settings → meaning A. When context is customer detail view → meaning B. The disambiguator is always which page/parent entity the request targets."_

{{/each}}
{{else}}
_(No structural trap zones auto-detected in the scanned code. This can mean: (a) your codebase genuinely has no ambiguous entity names, (b) the ambiguity is semantic — e.g., discriminator-value overload like `type = "department"` reusing an entity name — which the MVP detector does not catch, or (c) the scan paths excluded the relevant files.)_

_If you know of ambiguous terms the detector missed, add them here manually — the Logic Adversary will read them just the same._
{{/if}}

---

_Detector notes_: {{scanned.files}} files scanned, {{scanned.declarations}} entity-like declarations examined, {{trapZones.length}} trap zones flagged.
